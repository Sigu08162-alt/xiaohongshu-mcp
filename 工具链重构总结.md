# 工具链重构总结

## 问题诊断

### 原始问题
用户运行 `publish_content` MCP工具时，遇到错误：
```
❌ 标题填写失败: Element is not an <input>, <textarea> or [contenteditable] element
```

### 根本原因
1. **选择器不精确**: `.d-text` 匹配了多个元素，第一个不是input元素
2. **硬编码过多**:
   - URL硬编码（`target=image` 可能跳到文字配图页面）
   - 选择器提取逻辑硬编码（标题用placeholder判断）
3. **采集不完整**: 旧采集器只采集了6个字段，缺少位置、可见性等关键信息

---

## 解决方案

### 新工具链架构

```
旧版（有问题）:
  discover_pages → refresh_selectors → wiring_bootstrap.go（硬编码映射）→ MCP服务器

新版（无硬编码）:
  discover_pages → collect_metadata（完整采集50+字段）→ extract_selectors（智能提取）→ MCP服务器
```

### 核心改进

1. **完整元数据采集器** (`cmd/collect_metadata/main.go`)
   - 采集50+字段（位置、可见性、ARIA、data-*等）
   - 无差别采集所有元素
   - 无硬编码规则
   - 支持交互式操作（上传图片）

2. **智能选择器提取**（规划中）
   - 基于完整元数据，使用多特征综合判断
   - 标题输入框：placeholder + type + 位置 + 可见性
   - 内容编辑器：contenteditable + role + classes
   - 发布按钮：text + !disabled + 位置

3. **自适应性**
   - 页面改版后只需重新采集
   - 无需修改代码

---

## 新增文件

### 核心工具
- `cmd/collect_metadata/main.go` - 完整元数据采集器 ⭐️
- `toolchain_v2.sh` - 完整工具链脚本
- `test_metadata_collector.sh` - 快速测试脚本

### 文档
- `TOOLCHAIN_V2.md` - 完整工具链文档
- `工具链重构总结.md` - 本文档

### 临时诊断工具
- `cmd/debug_title_selector/main.go` - 选择器诊断工具
- `debug_title_selector` - 编译后的可执行文件

---

## 使用方法

### 快速测试

```bash
# 1. 测试发布页面元数据采集
./test_metadata_collector.sh

# 浏览器会打开发布页面
# 请手动上传一张图片
# 然后回到终端按Enter
# 脚本会自动分析并显示结果
```

### 完整工具链

```bash
# 运行完整工具链
./toolchain_v2.sh

# 按提示选择:
# 1. 系统类型（创作者/用户）
# 2. 交互模式（推荐选Y）
# 3. 在浏览器中手动操作（上传图片）
# 4. 回到终端按Enter继续
```

### 单独运行采集器

```bash
# 采集所有页面
./bin/collect_metadata \
  --input discovered_pages_creator.yaml \
  --output metadata_creator.yaml \
  --wait 5

# 仅采集发布页面
./bin/collect_metadata \
  --input discovered_pages_creator.yaml \
  --output metadata_publish.yaml \
  --page publish_publish \
  --wait 5

# 非交互模式（自动采集）
./bin/collect_metadata \
  --input discovered_pages_creator.yaml \
  --output metadata_creator.yaml \
  --no-interactive \
  --wait 5
```

---

## 元数据示例

### 旧版（不完整）

```yaml
inputs:
  - text: ""
    selector: .d-text
    placeholder: "填写标题会有更多赞哦～"
    type: text
    classes: [d-text]
    tag_name: input
```

**问题**: 只有6个字段，无法精确定位元素

### 新版（完整）

```yaml
elements:
  - tag_name: input
    id: ""
    classes: [d-input, large]
    name: title
    css_selector: "input.d-input"

    text: ""
    value: ""
    inner_html: ""

    type: text
    placeholder: "填写标题会有更多赞哦～"
    disabled: false
    readonly: false
    required: false

    role: ""
    aria_label: ""
    aria_attrs: {}

    data_attrs:
      data-testid: "title-input"
      data-component: "TitleInput"

    position:
      x: 120.5
      y: 200.0
      width: 600.0
      height: 40.0

    visible: true
    display: "block"
    visibility: "visible"
    opacity: "1"
    contenteditable: "false"

    parent_tag: div
    parent_classes: [form-group]
    children_count: 0

    other_attrs:
      autocomplete: "off"
```

**优势**: 50+字段，可以用多种方式精确定位

---

## 对比表

| 项目 | 旧版工具链 | 新版工具链 |
|------|-----------|-----------|
| 采集字段数 | 6个 | 50+ |
| 硬编码规则 | 多处硬编码 | 零硬编码 |
| 位置信息 | ❌ 无 | ✅ 有 (x,y,width,height) |
| 可见性 | ❌ 简单判断 | ✅ 完整 (visible, display, opacity) |
| ARIA属性 | ❌ 无 | ✅ 完整 |
| Data属性 | ❌ 无 | ✅ 完整 |
| 层级关系 | ❌ 无 | ✅ 有 (parent, children) |
| 交互支�� | ⚠️ 有限 | ✅ 完整支持 |
| 自适应性 | ❌ 改版需改代码 | ✅ 改版仅需重采集 |

---

## 下一步计划

### 短期（本周）
1. ✅ 完成元数据采集器
2. ⏳ 测试发布页面元数据采集
3. ⏳ 基于元数据手动提取正确的选择器
4. ⏳ 更新 `wiring_bootstrap.go` 使用新选择器

### 中期（下周）
1. 开发智能选择器提取器 (`extract_selectors`)
2. 实现自动化选择器更新
3. 添加选择器验证工具

### 长期
1. 集成机器学习模型识别元素
2. 自动化E2E测试
3. 监控小红书页面变化并自动更新

---

## 关键代码改动

### 新增
- `cmd/collect_metadata/main.go` - 372行，完整元数据采集器
- `toolchain_v2.sh` - 165行，完整工具链脚本
- `TOOLCHAIN_V2.md` - 完整文档

### 修改
- `wiring_bootstrap.go` - 改进选择器构建逻辑（临时方案，待智能提取器完成后替换）

### 废弃
- `cmd/refresh_selectors/` - 旧采集器（保留供参考）
- `collect_all.sh` - 旧工具链脚本（保留供参考）

---

## FAQ

**Q: 为什么不直接修复旧的采集器？**

A: 旧采集器设计理念有问题（硬编码 + 采集不完整）。修修补补不如重新设计一个无硬编码、完整采集的新工具链。

**Q: 新工具链性能如何？**

A: 采集更慢（因为采集更多数据），但这是一次性成本。采集完成后生成的选择器文件可以长期使用。

**Q: 什么时候启用新工具链？**

A:
1. 现在可以测试新工具链
2. 验证采集的元数据完整性
3. 基于元数据手动或自动提取选择器
4. 更新MCP服务器配置
5. 废弃旧工具链

**Q: 如果采集失败怎么办？**

A:
1. 使用交互模式（推荐）
2. 手动操作页面（如上传图片）
3. 等待页面完全加载后再继续
4. 检查Cookie是否有效

---

## 测试清单

- [ ] 运行 `./test_metadata_collector.sh` 测试发布页面
- [ ] 验证是否采集到标题输入框元数据
- [ ] 验证是否采集到内容编辑器元数据
- [ ] 验证是否采集到发布按钮元数据
- [ ] 运行完整工具链 `./toolchain_v2.sh`
- [ ] 采集所有创作者页面
- [ ] 查看元数据文件内容
- [ ] 基于元数据提取选择器
- [ ] 更新MCP服务器配置
- [ ] 测试发布功能

---

## 贡献者

- 初始设计: Claude (Sonnet 4.5)
- 需求提出: 用户（"设计一条工具链:1.采集脚本--无差别加载所有检测到的链接页面的所有元素组件的所有完整的元数据信息"）

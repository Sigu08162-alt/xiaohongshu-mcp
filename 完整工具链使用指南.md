# 完整自动化工具链使用指南

## 🎯 工具链概述

本项目实现了从页面发现 → 组件采集 → MCP服务使用的完整自动化流程：

```
1. discover_pages        →  discovered_pages*.yaml
2. refresh_selectors     →  selectors_discovered_pages*.yaml
3. MCP服务器自动加载    →  直接使用最新采集结果
```

**无需手动转换配置文件**，形成完整闭环！

---

## 📦 工具链组件

### 1. 页面发现工具 (discover_pages)

**功能**: 自动发现小红书页面链接

**输入**: 无（访问起始页）
**输出**: `discovered_pages*.yaml`

```bash
./bin/discover_pages --system creator --no-interactive --wait 8
```

### 2. 组件采集工具 (refresh_selectors)

**功能**: 采集页面所有可交互组件（按钮、输入框等）

**输入**: `discovered_pages*.yaml`
**输出**: `selectors_discovered_pages*.yaml`

```bash
./bin/refresh_selectors --pages discovered_pages_creator.yaml --no-interactive --wait 6
```

### 3. MCP服务器 (xiaohongshu-mcp)

**功能**: 提供MCP协议接口，支持自动化发布等操作

**输入**: 自动加载 `selectors_discovered_pages*.yaml`
**输出**: MCP工具调用接口

```bash
./bin/xiaohongshu-mcp
```

### 4. 一键采集脚本 (collect_all.sh)

**功能**: 登录 + 发现 + 采集 一条龙

```bash
./collect_all.sh
```

---

## 🔄 完整工作流

### 方案一：首次使用（推荐）

```bash
# 1. 一键采集（包含登录、发现、采集）
./collect_all.sh

# 选择: 1 (创作者系统)
# 发布页面: Y (交互模式，手动上传图片)

# 2. 启动MCP服务器
./bin/xiaohongshu-mcp

# 服务器会自动输出:
# 📂 找到选择器文件: selectors_discovered_pages_creator.yaml
# 📦 检测到采集器生成的选择器文件，正在提取发布页面选择器...
#   ✓ upload_input: input
#   ✓ title_input: .d-text (placeholder: 填写标题会有更多赞哦～)
#   ✓ content: .tiptap (富文本编辑器)
#   ✓ submit: button.d-button (text: 发布)
#   ✓ save_draft: button.d-button (text: 暂存离开)
```

### 方案二：分步执行

```bash
# 1. 登录（如果需要）
./login.sh

# 2. 发现页面
./bin/discover_pages --system creator --no-interactive --wait 8

# 3. 采集组件（发布页面需要交互模式）
./bin/refresh_selectors --pages discovered_pages.yaml --wait 6

# 4. 启动MCP服务器
./bin/xiaohongshu-mcp
```

### 方案三：只更新选择器

```bash
# 如果小红书页面更新了，只需重新采集
./bin/refresh_selectors --pages discovered_pages.yaml --no-interactive --wait 6

# MCP服务器重启后会自动加载新的选择器
./bin/xiaohongshu-mcp
```

---

## 🔧 选择器文件自动加载机制

### 加载优先级

MCP服务器按以下顺序查找选择器文件：

1. **环境变量指定**: `XHS_SELECTORS_PATH`
2. **自动查找**（按优先级）:
   - `selectors_discovered_pages_creator.yaml` ← **优先**（采集器生成）
   - `selectors_discovered_pages.yaml` ← 次优（采集器生成，旧命名）
   - `config.yaml` ← 备用（传统配置）

### 文件格式自动识别

系统会自动识别两种格式：

**格式1：采集器生成（推荐）**
```yaml
version: 1.0.0
generated: "2026-01-31T12:23:28+08:00"
pages:
  publish_publish:
    buttons:
      - text: "发布"
        selector: "button.d-button"
    inputs:
      - placeholder: "填写标题会有更多赞哦～"
        selector: ".d-text"
```

**格式2：传统配置（备用）**
```yaml
selectors:
  publish:
    upload_input: ".upload-input"
    title_input: "div.d-input input"
```

---

## 🎛️ 环境变量配置

### XHS_SELECTORS_PATH

**用途**: 指定选择器文件路径

```bash
# 使用自定义选择器文件
export XHS_SELECTORS_PATH=/path/to/custom_selectors.yaml
./bin/xiaohongshu-mcp
```

### XHS_CONFIG_PATH

**用途**: 指定主配置文件路径（超时、URL等）

```bash
# 使用自定义配置
export XHS_CONFIG_PATH=/path/to/custom_config.yaml
./bin/xiaohongshu-mcp
```

---

## 📊 采集结果文件说明

### discovered_pages*.yaml

**内容**: 发现的页面URL列表

**用途**: 作为 refresh_selectors 的输入

**示例**:
```yaml
version: 1.0.0
generated: "2026-01-31T12:23:28+08:00"
home_page: https://creator.xiaohongshu.com/new/home?source=official
links:
  publish_publish:
    text: 发布笔记
    url: https://creator.xiaohongshu.com/publish/publish?source=official
    category: publish
```

### selectors_discovered_pages*.yaml

**内容**: 页面组件详细信息（按钮、输入框、容器等）

**用途**: MCP服务器自动加载，提取选择器

**大小**: 通常几百KB（包含完整的DOM结构信息）

**示例**:
```yaml
version: 1.0.0
generated: "2026-01-31T12:23:28+08:00"
pages:
  publish_publish:
    page_name: publish_publish
    url: https://creator.xiaohongshu.com/publish/publish?source=official&from=tab_switch
    buttons:
      - text: 发布
        selector: button.d-button
        classes: [d-button, d-button-primary]
        visible: true
    inputs:
      - placeholder: 填写标题会有更多赞哦～
        selector: .d-text
        type: text
        tag_name: input
        visible: true
      - selector: .tiptap
        tag_name: div
        classes: [tiptap, ProseMirror]
        visible: true
```

---

## ✅ 验证工具链

### 测试选择器加载

```bash
./test_selector_loading.sh
```

**预期输出**:
```
✅ 成功解析采集器格式
   版本: 1.0.0
   生成时间: 2026-01-31T12:23:28+08:00
   页面数量: 5

🔍 提取选择器:
  ✓ upload_input: input
  ✓ title_input: .d-text (placeholder: 填写标题会有更多赞哦～)
  ✓ content: .tiptap (富文本编辑器)
  ✓ submit: button.d-button (text: 发布)
  ✓ save_draft: button.d-button (text: 暂存离开)

✅ 所有必需选择器都已提取
```

---

## 🔄 维护流程

### 定期更新选择器

**推荐频率**: 每周或页面更新后

```bash
# 1. 重新采集
./collect_all.sh

# 2. 重启MCP服务器
# MCP服务器会自动加载最新的 selectors_discovered_pages_creator.yaml
```

### Cookie过期处理

```bash
# 重新登录
./login.sh

# 重新采集（Cookie自动使用最新的）
./bin/refresh_selectors --pages discovered_pages.yaml --no-interactive
```

---

## 🎯 最佳实践

1. **版本控制**:
   - 将 `discovered_pages*.yaml` 和 `selectors_*.yaml` 纳入git
   - 记录每次采集的时间和变化

2. **自动化**:
   - 使用 `collect_all.sh` 一键完成采集
   - 设置定时任务每周更新选择器

3. **备份**:
   - 采集前会自动备份旧配置（config.yaml.backup）
   - selectors文件较大，建议定期清理旧版本

4. **测试**:
   - 每次采集后运行 `test_selector_loading.sh` 验证
   - MCP服务器启动时会显示加载的选择器信息

---

## 🐛 故障排查

### Q: MCP服务器找不到选择器文件

**A**: 检查文件是否存在
```bash
ls -lh selectors_discovered_pages_creator.yaml

# 如果不存在，重新采集
./bin/refresh_selectors --pages discovered_pages.yaml --wait 6
```

### Q: 提取的选择器不完整

**A**: 检查发布页面采集是否成功
```bash
# 查看采集结果
grep -A 20 "publish_publish:" selectors_discovered_pages_creator.yaml

# 如果输入框太少，使用交互模式重新采集
./bin/refresh_selectors --pages discovered_pages.yaml --page publish_publish --wait 6
# 按提示手动上传图片
```

### Q: 想使用旧的config.yaml格式

**A**: 删除采集器生成的文件
```bash
mv selectors_discovered_pages_creator.yaml selectors_discovered_pages_creator.yaml.bak

# MCP服务器会回退到使用 config.yaml
./bin/xiaohongshu-mcp
```

---

## 📚 相关文档

- **双系统使用指南.md**: 创作者系统 vs 用户系统
- **采集结果报告.md**: 最近一次采集的详细分析
- **config.yaml**: 传统配置文件（超时、URL等）

---

## 🎉 总结

通过这套完整的工具链，你可以：

✅ **自动化**: 一键完成页面发现和组件采集
✅ **零转换**: MCP服务器直接读取采集结果
✅ **易维护**: 页面更新后重新采集即可
✅ **向后兼容**: 仍支持传统config.yaml格式

享受自动化带来的便利！🚀

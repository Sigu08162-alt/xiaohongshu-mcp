# 小红书双系统采集工具使用指南

## 系统说明

工具现在支持小红书的两个独立系统：

### 1. Creator System (创作者系统)
- **域名**: `creator.xiaohongshu.com`
- **用户**: 内容创作者
- **功能**: 发布笔记、数据分析、笔记管理、粉丝数据等
- **起始页**: https://creator.xiaohongshu.com/new/home?source=official

### 2. User System (普通用户系统)
- **域名**: `www.xiaohongshu.com`
- **用户**: 普通浏览用户
- **功能**: 发现内容、关注动态、搜索、购物等
- **起始页**: https://www.xiaohongshu.com/explore

---

## 快速开始

### 方案一：采集创作者系统 (默认)

```bash
# 1. 发现创作者页面
./bin/discover_pages --system creator --no-interactive --wait 8

# 2. 采集组件信息
./bin/refresh_selectors --pages discovered_pages.yaml --no-interactive --wait 6
```

**发现的页面**:
- 发布笔记 (`/publish/publish`)
- 笔记管理 (`/creator/content`)
- 数据看板 (`/creator/data-board`)
- 内容分析 (`/creator/content/data`)
- 粉丝数据 (`/creator/fans`)

### 方案二：采集普通用户系统

```bash
# 1. 发现用户系统页面
./bin/discover_pages --system user --no-interactive --wait 8 --output discovered_pages_user.yaml

# 2. 采集组件信息
./bin/refresh_selectors --pages discovered_pages_user.yaml --no-interactive --wait 6 --output selectors_user.yaml
```

**发现的页面**:
- 发现 (`/explore`)
- 搜索 (`/search_result`)
- 关注 (`/followFeed`) - 如果页面文本中包含
- 我的主页 (`/user/profile`) - 如果页面文本中包含
- 消息 (`/chat`) - 如果页面文本中包含

### 方案三：同时采集两个系统

```bash
# 采集创作者系统
./bin/discover_pages --system creator --output discovered_creator.yaml --no-interactive
./bin/refresh_selectors --pages discovered_creator.yaml --output selectors_creator.yaml --no-interactive

# 采集用户系统
./bin/discover_pages --system user --output discovered_user.yaml --no-interactive
./bin/refresh_selectors --pages discovered_user.yaml --output selectors_user.yaml --no-interactive
```

---

## 命令参数

### discover_pages

| ��数 | 默认值 | 说明 |
|------|--------|------|
| `--system` | `creator` | 系统类型: `creator` 或 `user` |
| `--home` | 自动 | 起始页URL (根据system自动设置) |
| `--output` | `discovered_pages.yaml` | 输出文件路径 |
| `--wait` | 5 | 页面加载等待秒数 |
| `--no-interactive` | false | 非交互模式 |
| `--cookies` | 自动查找 | Cookie文件路径 |

### refresh_selectors

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--pages` | `discovered_pages.yaml` | 输入页面列表YAML |
| `--output` | `selectors_all_pages.yaml` | 输出文件路径 |
| `--wait` | 3 | 每页等待秒数 |
| `--no-interactive` | false | 非交互模式 |
| `--page` | - | 仅采集单个页面 |

---

## Cookie管理

两个系统使用相同的Cookie文件(`cookies.json`)，因为登录状态在`xiaohongshu.com`域名下共享。

```bash
# 重新登录（当Cookie过期时）
./login.sh
```

Cookie自动查找顺序:
1. `./cookies.json`
2. `~/.xiaohongshu/cookies.json`
3. `./xiaohongshu_cookies.json`

---

## 输出文件说明

### discovered_pages.yaml
页面发现结果，包含：
- URL列表
- 页面文本描述
- 类别分类
- 来源标记(from-dom/inferred)

### selectors_all_pages.yaml
组件采集结果，包含：
- 按钮 (buttons)
- 输入框 (inputs)
- 容器 (containers)
- 链接 (links)

---

## 使用场景

### 创作者系统场景
- 自动化发布笔记
- 数据分析和监控
- 笔记管理批量操作
- 粉丝数据导出

### 用户系统场景
- 内容浏览自动化
- 搜索结果采集
- 关注动态监控
- 购物功能测试

---

## 故障排查

### Q: 发现的页面数量为0
**A**: 增加`--wait`参数，给页面更多加载时间
```bash
./bin/discover_pages --system user --wait 15
```

### Q: 所有页面跳转到登录
**A**: Cookie过期，重新登录
```bash
./login.sh
```

### Q: user系统发现的页面太少
**A**: 普通用户系统的导航可能需要登录后才能看到更多链接，或者页面使用了特殊的路由方式

---

## 技术细节

### 创作者系统链接发现策略
1. 扫描`<a>`标签中包含`creator.xiaohongshu.com`的链接
2. 从`.menu-item`等菜单元素获取真实URL
3. 基于页面文本关键词推测URL
4. 自动补全`?source=official`参数

### 用户系统链接发现策略
1. 扫描`nav`, `header`中的导航链接
2. 基于常见页面路径推测(/explore, /followFeed等)
3. 不添加source参数(用户系统不需要)

---

## 最佳实践

1. **定期更新**: 每周运行discover_pages更新页面列表
2. **Cookie维护**: Cookie过期时及时重新登录
3. **分离输出**: 两个系统使用不同的输出文件避免混淆
4. **版本控制**: 将YAML文件纳入git跟踪变化
5. **等待时间**: 网络慢时增加`--wait`参数

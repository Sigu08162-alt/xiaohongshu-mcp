## 1. Implementation
- [ ] 扩展 `PublishRequest` 与 `PublishImageContent`（含领域模型与用例层映射）
- [ ] 在发布流程中透传 `location` 与 `marker_tags`
- [ ] 更新 Swagger 生成与 API 文档示例

## 2. Testing
- [ ] 手工验证发布图文时地点与标记可正常设置
- [ ] 回归验证不传新字段时发布流程不受影响
